<!-- Paritials head -->
 
<%- include('../partials/head.ejs', { extraFont: "https://fonts.googleapis.com/css2?family=Fjalla+One&display=swap", stylesheetPath: "/css/styles-register.css" }) %>

<body>
    <!--Partials header -->
    <%- include('../partials/header.ejs') %>

    <main>
        
        <!----------------- FORMULARIO DE REGISTRO ------------------------>
        <section class="fondo-register">
            <article class="contenedor-register">
                <form action="/user/register" method="post">

                    <!----------------- TITULO DEL FORMULARIO ------------------------>
                    <label class="titulo-register">Registrarse</label>

                    <!----------------- CAMPOS DEL FORMULARIO ------------------------>
                    <label for="user">Usuario</label>
                    <input type="text" id="user" name="user" value="<%= locals.oldData ? oldData.user : null %>">
                    <% if (locals.errors && errors.user) { %>
                        <div class="error">* <%= errors.user.msg %></div>
                    <% } %>
                    
                    <label for="email">Email</label>
                    <input type="text" id="email" name="email" value="<%= locals.oldData ? oldData.email : null %>">
                    <% if (locals.errors && errors.email) { %>
                        <div class="error">* <%= errors.email.msg %></div>
                    <% } %>

                    <label for="password">Contraseña</label>
                    <input type="password" id="password" name="password" value="<%= locals.oldData ? oldData.password : null %>">
                    <% if (locals.errors && errors.password) { %>
                        <div class="error">* <%= errors.password.msg %></div>
                    <% } %>

                    <label for="repassword">Confirmar contraseña</label>
                    <input type="password" id="repassword" name="repassword" value="<%= locals.oldData ? oldData.repassword : null %>">
                    <% if (locals.errors && errors.repassword) { %>
                        <div class="error">* <%= errors.repassword.msg %></div>
                    <% } %>

                    <!----------------- VOLVER A LA PAGINA DE LOGIN ------------------------>
                    <a href="/user/login">¿Ya tienes cuenta? Ingresa</a>

                    <!----------------- BOTON DE REGISTRARSE ------------------------>
                    <button type="submit" class="registrarse">Registrarse</button>
                </form>
            </article>

            <!----------------- FRASE PARA RECIBIR A LOS USUARIOS QUE SE REGISTRAN ------------------------>
            <article class="frase-register">
                <p>Explora un mar de posibilidades tecnológicas al abrir la puerta a nuestro mundo digital.</p>
            </article>
        </section>
    </main>

    <!--Partials footer  -->
    <%- include('../partials/footer.ejs') %>
</body>
</html>